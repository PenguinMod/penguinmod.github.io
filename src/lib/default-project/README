`dango.svg` is from Twitter's Twemoji (CC BY 4.0 https://creativecommons.org/licenses/by/4.0/)

If `default_project.pmf` in the repository root is a non-empty file, it will be used instead of the built-in default project.